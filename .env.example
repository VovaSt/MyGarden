# PostgreSQL connection used by Prisma and the NestJS backend.
# Replace the password before starting local development.
DATABASE_URL="postgresql://mygarden:change-me@localhost:5432/mygarden?schema=public"

# Browser origin permitted to call the local API during Angular development.
FRONTEND_ORIGIN="http://localhost:4200"
